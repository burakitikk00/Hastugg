# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# PostgreSQL connection string (Supabase veya başka bir PostgreSQL sunucusu)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Local: postgresql://user:password@localhost:5432/database
# Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@host:port/database

# Alternatif olarak ayrı ayrı database bilgileri (DATABASE_URL yerine kullanılabilir)
# DB_HOST=your-database-host
# DB_PORT=5432
# DB_USER=your-database-user
# DB_PASSWORD=your-database-password
# DB_DATABASE=your-database-name

# Supabase Configuration
# Supabase dashboard'dan alabilirsiniz: Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# Supabase Storage Configuration
# Storage bucket adı (varsayılan: images)
# Supabase dashboard'dan Storage > Buckets bölümünden bucket oluşturmanız gerekiyor
SUPABASE_STORAGE_BUCKET=images

# JWT Secret (Güçlü bir rastgele string kullanın)
# Örnek: openssl rand -base64 32 komutu ile oluşturabilirsiniz
JWT_SECRET=your-very-secure-jwt-secret-key-here

# CORS Configuration
# Frontend URL'lerini virgülle ayırarak ekleyin
# Local development için otomatik olarak localhost:5173 ve localhost:3000 eklenir
# Production için sadece burada belirtilen URL'ler izin verilir
# Örnek: https://your-app.vercel.app,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,https://your-vercel-app.vercel.app

# Not: Email ayarları (EMAIL_USER, EMAIL_PASS) veritabanında saklanır,
# bu yüzden environment variable olarak tanımlanmaz.
# Email ayarlarını admin panelinden yapılandırabilirsiniz.
